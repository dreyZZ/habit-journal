<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HabitMemories" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:forceDarkAllowed">true</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">#1B132C</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Text Colors -->
        <item name="android:textColor">#0E0E0E</item>
        <item name="android:textColorPrimary">#0E0E0E</item>
        <item name="android:textColorSecondary">#0E0E0E</item>
        <item name="android:textColorTertiary">#0E0E0E</item>
        <item name="android:colorControlNormal">#0E0E0E</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">#595959</item>
        <item name="colorSecondaryVariant">@color/black</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.HabitMemories.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.HabitMemories.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.HabitMemories.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>