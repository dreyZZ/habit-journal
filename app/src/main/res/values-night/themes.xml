<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HabitMemories" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/main_background_color</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Text Colors -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/main_background_color</item>
        <item name="android:textColorTertiary">@color/main_background_color</item>
        <item name="android:colorControlNormal">@color/main_background_color</item>
        <item name="android:actionMenuTextColor">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">#595959</item>
        <item name="colorSecondaryVariant">@color/black</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
    <!-- Customize your theme here. -->
    </style>
</resources>